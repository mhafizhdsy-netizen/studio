
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the HitunginAja application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user entity."
        },
        "name": {
          "type": "string",
          "description": "The user's display name."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "photoURL": {
          "type": "string",
          "description": "The URL of the user's profile picture.",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was created.",
          "format": "date-time"
        },
        "onboardingCompleted": {
            "type": "boolean",
            "description": "Flag to check if user has completed the onboarding flow."
        },
        "isAdmin": {
          "type": "boolean",
          "description": "Flag to check if the user has admin privileges."
        },
        "isSuspended": {
          "type": "boolean",
          "description": "Flag to check if the user's account is suspended."
        }
      },
      "required": [
        "id",
        "name",
        "email"
      ]
    },
    "Calculation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Calculation",
      "type": "object",
      "description": "Represents a single HPP calculation performed by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the calculation entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to User. (Relationship: User 1:N Calculation)"
        },
        "productName": {
          "type": "string",
          "description": "The name of the product for which HPP was calculated."
        },
        "productImageUrl": {
          "type": "string",
          "description": "The URL for the product's image.",
          "format": "uri"
        },
        "productDescription": {
          "type": "string",
          "description": "A description of the product, often generated by AI."
        },
        "materials": {
          "type": "array",
          "description": "A list of materials associated with the product.",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "cost": { "type": "number" },
              "qty": { "type": "number" },
              "unit": { "type": "string" },
              "description": { "type": "string" },
              "isTotalCost": { "type": "boolean" },
              "purchaseLink": { "type": "string", "format": "uri" }
            },
             "required": ["name", "cost", "qty"]
          }
        },
        "laborCost": {
          "type": "number",
          "description": "The labor cost associated with producing the product."
        },
        "overhead": {
          "type": "number",
          "description": "Overhead costs associated with producing the product."
        },
        "packaging": {
          "type": "number",
          "description": "Packaging costs associated with the product."
        },
        "productQuantity": {
          "type": "number",
          "description": "The total number of products produced in one batch."
        },
        "productionTips": {
          "type": "string",
          "description": "User-provided tips related to the production of the product."
        },
        "totalHPP": {
          "type": "number",
          "description": "The total HPP calculated for the product."
        },
        "suggestedPrice": {
          "type": "number",
          "description": "The suggested selling price for the product."
        },
        "margin": {
          "type": "number",
          "description": "The profit margin percentage."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the calculation was performed.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "productName",
        "totalHPP",
        "suggestedPrice",
        "margin",
        "createdAt"
      ]
    },
    "PublicCalculation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PublicCalculation",
      "type": "object",
      "description": "Represents a calculation shared publicly by a user, excluding personal data.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the public calculation entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the original User owner."
        },
        "calculationId": {
          "type": "string",
          "description": "Reference to the original Calculation."
        },
        "isFeatured": {
            "type": "boolean",
            "description": "Flag to check if the calculation is featured by an admin."
        },
        "productName": {
          "type": "string",
          "description": "The name of the product."
        },
        "productImageUrl": {
          "type": "string",
          "description": "The URL for the product's image.",
          "format": "uri"
        },
        "productDescription": {
          "type": "string",
          "description": "A description of the product."
        },
        "totalHPP": {
          "type": "number",
          "description": "The total HPP calculated for the product."
        },
        "suggestedPrice": {
          "type": "number",
          "description": "The suggested selling price for the product."
        },
        "margin": {
          "type": "number",
          "description": "The profit margin percentage."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the calculation was performed.",
          "format": "date-time"
        },
         "materials": {
          "type": "array",
          "description": "A list of materials associated with the product.",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string" },
              "cost": { "type": "number" },
              "qty": { "type": "number" },
              "unit": { "type": "string" },
              "description": { "type": "string" },
              "isTotalCost": { "type": "boolean" },
              "purchaseLink": { "type": "string", "format": "uri" }
            },
            "required": ["name", "cost", "qty"]
          }
        },
        "productionTips": {
          "type": "string",
          "description": "User-provided tips related to the production of the product."
        }
      },
      "required": [
        "id",
        "userId",
        "calculationId",
        "productName",
        "totalHPP",
        "suggestedPrice",
        "margin",
        "createdAt"
      ]
    },
    "Comment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Comment",
      "type": "object",
      "description": "Represents a comment made by a user on a public calculation.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the comment."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who made the comment."
        },
        "userName": {
          "type": "string",
          "description": "The display name of the user."
        },
        "userPhotoURL": {
          "type": "string",
          "description": "The photo URL of the user.",
          "format": "uri"
        },
        "text": {
          "type": "string",
          "description": "The content of the comment."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the comment was created.",
          "format": "date-time"
        },
        "parentId": {
          "type": "string",
          "description": "The ID of the parent comment if this is a reply."
        }
      },
      "required": [
        "id",
        "userId",
        "userName",
        "text",
        "createdAt"
      ]
    },
    "Report": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "Report",
        "type": "object",
        "description": "Represents a user-submitted report for a piece of content.",
        "properties": {
            "id": { "type": "string" },
            "calculationId": { "type": "string", "description": "FK to the public_calculations table." },
            "reporterUserId": { "type": "string", "description": "FK to the users table (who reported)." },
            "reportedUserId": { "type": "string", "description": "FK to the users table (who was reported)." },
            "category": { "type": "string", "enum": ["Konten Seksual", "Ujaran Kebencian", "Kekerasan", "Spam", "Pelanggaran Hak Cipta", "Lainnya"] },
            "reason": { "type": "string", "description": "The detailed reason for the report." },
            "status": { "type": "string", "enum": ["pending", "resolved", "dismissed"], "default": "pending" },
            "createdAt": { "type": "string", "format": "date-time" }
        },
        "required": ["id", "calculationId", "reporterUserId", "reportedUserId", "category", "status", "createdAt"]
    },
    "ChatSession": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChatSession",
      "type": "object",
      "description": "Represents a single anonymous chat session between two users.",
      "properties": {
        "participantIds": {
          "type": "array",
          "items": { "type": "string" },
          "description": "An array containing the UIDs of the two participants."
        },
        "status": {
          "type": "string",
          "enum": ["pending", "active", "ended"],
          "description": "The current status of the chat session."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the session was created."
        }
      },
      "required": ["participantIds", "status", "createdAt"]
    },
    "ChatMessage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChatMessage",
      "type": "object",
      "description": "Represents a single message within an anonymous chat session.",
      "properties": {
        "senderId": {
          "type": "string",
          "description": "The UID of the user who sent the message."
        },
        "content": {
          "type": "object",
          "properties": {
            "text": { "type": "string" },
            "imageUrl": { "type": "string", "format": "uri" },
            "calculation": { "type": "object" }
          },
          "description": "The multi-format content of the message."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the message was sent."
        }
      },
      "required": ["senderId", "content", "createdAt"]
    },
    "Expense": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Expense",
      "type": "object",
      "description": "Represents a single operational expense entry.",
      "properties": {
        "id": { "type": "string", "description": "Unique ID for the expense." },
        "userId": { "type": "string", "description": "The UID of the user who logged the expense." },
        "name": { "type": "string", "description": "Name or description of the expense." },
        "amount": { "type": "number", "description": "The cost of the expense." },
        "category": { 
          "type": "string", 
          "description": "The category of the expense.",
          "enum": ["Sewa Tempat", "Listrik", "Gaji Karyawan", "Biaya Pengemasan", "Lainnya"]
        },
        "date": { "type": "string", "format": "date-time", "description": "The date the expense was incurred." }
      },
      "required": ["id", "userId", "name", "amount", "category", "date"]
    },
    "ProfitReport": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "ProfitReport",
        "type": "object",
        "description": "Represents an aggregated profit analysis report for a specific period.",
        "properties": {
            "id": { "type": "string", "description": "Unique ID for the report, e.g., YYYY-MM." },
            "userId": { "type": "string", "description": "The UID of the user." },
            "month": { "type": "string", "description": "The month of the report in YYYY-MM format." },
            "totalProductionCost": { "type": "number", "description": "Sum of all HPP from calculations in that month." },
            "totalOperationalCost": { "type": "number", "description": "Sum of all expenses in that month." },
            "totalRevenue": { "type": "number", "description": "Sum of all suggested prices from calculations in that month." },
            "estimatedProfit": { "type": "number", "description": "Total Revenue - (Total Production Cost + Total Operational Cost)." },
            "averageMargin": { "type": "number", "description": "Average profit margin from calculations." },
            "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp when the report was generated." }
        },
        "required": [
            "id",
            "userId",
            "month",
            "totalProductionCost",
            "totalOperationalCost",
            "totalRevenue",
            "estimatedProfit",
            "averageMargin",
            "createdAt"
        ]
    },
     "AIChatMessage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AIChatMessage",
      "type": "object",
      "description": "Represents a single message in a conversation with the AI Business Consultant.",
      "properties": {
        "role": {
          "type": "string",
          "enum": ["user", "model"],
          "description": "The role of the message sender (user or AI model)."
        },
        "content": {
          "type": "string",
          "description": "The text content of the message."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the message was created."
        }
      },
      "required": ["role", "content", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. The 'userId' parameter corresponds to the Firebase Auth UID.  Authorization is based on path ownership: only the authenticated user can access their own profile.",
          "params": [
            {
              "name": "userId",
              "description": "The Firebase Auth UID of the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/calculations/{calcId}",
        "definition": {
          "entityName": "Calculation",
          "schema": {
            "$ref": "#/backend/entities/Calculation"
          },
          "description": "Stores calculation history for each user. The 'userId' parameter corresponds to the Firebase Auth UID. Authorization is based on path ownership: only the authenticated user can access their own calculations.  Includes the 'userId' field to enforce ownership invariants.",
          "params": [
            {
              "name": "userId",
              "description": "The Firebase Auth UID of the user."
            },
            {
              "name": "calcId",
              "description": "The unique ID of the calculation."
            }
          ]
        }
      },
       {
        "path": "/users/{userId}/ai_consultant_history/{messageId}",
        "definition": {
          "entityName": "AIChatMessage",
          "schema": { "$ref": "#/backend/entities/AIChatMessage" },
          "description": "Stores the AI consultant chat history for a specific user. Access is restricted to the user.",
          "params": [
            { "name": "userId", "description": "The Firebase Auth UID of the user." },
            { "name": "messageId", "description": "The unique ID of the chat message." }
          ]
        }
      },
      {
        "path": "/public_calculations/{calcId}",
        "definition": {
          "entityName": "PublicCalculation",
          "schema": {
            "$ref": "#/backend/entities/PublicCalculation"
          },
          "description": "Stores publicly shared calculation data. No user-identifiable information is stored here to maintain privacy. Anyone can read these calculations, but only the original user (verified via a server-side function or callable) can create/delete/modify them.",
          "params": [
            {
              "name": "calcId",
              "description": "The unique ID of the public calculation."
            }
          ]
        }
      },
       {
        "path": "/public_calculations/{calcId}/comments/{commentId}",
        "definition": {
          "entityName": "Comment",
          "schema": {
            "$ref": "#/backend/entities/Comment"
          },
          "description": "Stores comments for a public calculation. Anyone can read, but only authenticated users can write. The 'calcId' parameter corresponds to the ID of the public calculation.",
          "params": [
            {
              "name": "calcId",
              "description": "The unique ID of the public calculation."
            },
            {
              "name": "commentId",
              "description": "The unique ID of the comment."
            }
          ]
        }
      },
      {
        "path": "/reports/{reportId}",
        "definition": {
          "entityName": "Report",
          "schema": { "$ref": "#/backend/entities/Report" },
          "description": "Stores user-submitted content reports. Only admins can read. Authenticated users can create.",
          "params": [
            { "name": "reportId", "description": "The unique ID of the report." }
          ]
        }
      },
      {
        "path": "/chat_sessions/{sessionId}",
        "definition": {
          "entityName": "ChatSession",
          "schema": { "$ref": "#/backend/entities/ChatSession" },
          "description": "Stores metadata for an anonymous chat session. Access is restricted to participants.",
          "params": [{ "name": "sessionId", "description": "The unique ID of the chat session." }]
        }
      },
      {
        "path": "/chat_sessions/{sessionId}/messages/{messageId}",
        "definition": {
          "entityName": "ChatMessage",
          "schema": { "$ref": "#/backend/entities/ChatMessage" },
          "description": "Stores messages for a specific anonymous chat session.",
          "params": [
            { "name": "sessionId", "description": "The unique ID of the chat session." },
            { "name": "messageId", "description": "The unique ID of the message." }
          ]
        }
      },
      {
        "path": "/users/{userId}/expenses/{expenseId}",
        "definition": {
            "entityName": "Expense",
            "schema": { "$ref": "#/backend/entities/Expense" },
            "description": "Stores operational expense entries for a user.",
            "params": [
                { "name": "userId", "description": "The Firebase Auth UID of the user." },
                { "name": "expenseId", "description": "The unique ID of the expense." }
            ]
        }
      },
      {
          "path": "/users/{userId}/reports/{reportId}",
          "definition": {
              "entityName": "ProfitReport",
              "schema": { "$ref": "#/backend/entities/ProfitReport" },
              "description": "Stores generated profit analysis reports for a user.",
              "params": [
                  { "name": "userId", "description": "The Firebase Auth UID of the user." },
                  { "name": "reportId", "description": "The unique ID of the report (e.g., YYYY-MM)." }
              ]
          }
      }
    ],
    "reasoning": "The Firestore structure is updated to support the AI Consultant feature by adding a new collection to store the chat history for each user."
  }
}
