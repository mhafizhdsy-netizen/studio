
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This file is now managed by Supabase Row Level Security.
    // The rules below are kept for reference but are no longer active for the database.

    /*
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    match /users/{userId}/calculations/{calculationId} {
      allow read, write, delete: if request.auth != null && request.auth.uid == userId;
    }

    match /public_calculations/{calculationId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null && request.auth.uid == resource.data.userId;
    }
    */
  }
}
